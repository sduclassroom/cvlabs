/* x5-2
Create a two-dimensional matrix with three channels of type byte with data size
100x100, and set all the values to 0.

Use the cv::Mat element access functions to modify the pixels.
Draw a green rectangle between (20,5) and (40,20) without using drawing functions.
*/
#include <opencv2/opencv.hpp>
using namespace cv;

int main(int argc, char*argv[]) {
    // your code here
    return 0;
}
